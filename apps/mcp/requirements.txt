# MCP Server dependencies
# Using mcp Python SDK (Model Context Protocol)
mcp>=0.9.0
httpx==0.27.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Python 3.11+ has built-in asyncio support, no need for asyncio-compat
